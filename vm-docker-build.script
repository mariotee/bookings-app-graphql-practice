#!/bin/bash

docker stop web
docker stop srv
docker rm srv
docker rm web
docker build -t namespace/srv ./server
docker run -d --name srv -p internal_ip:9000:4000 namespace/srv
docker build -t namespace/web ./client
docker run -d --name web -p internal_ip:5000:5000 namespace/web
